# ot-flow
"OT-Flow" — a lightweight middleware platform that sits on top of existing (or paper-based) hospital processes, using low-cost sensors + computer vision + RFID/NFC + a mobile/web app to create real-time visibility without ripping out existing infrastructure.
